name: Build and push dotnet image

on: 
  push:
    branches:
      - dev
      - test
      - stage
    paths:
      - 'images/runner-dotnet/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/Checkout@v5
      