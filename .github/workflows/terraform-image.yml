name: Build and push terraform image

on: 
  push:
    branches:
      - dev
      - test
      - stage
    paths:
      - 'images/runner-terraform/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/Checkout@v5
      