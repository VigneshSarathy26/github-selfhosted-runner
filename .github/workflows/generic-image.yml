name: Build and push generic image

on: 
  push:
    branches:
      - dev
      - test
      - stage
    paths:
      - 'images/runner-generic/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/Checkout@v5
      