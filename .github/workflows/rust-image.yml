name: Build and push rust image

on: 
  push:
    branches:
      - dev
      - test
      - stage
    paths:
      - 'images/runner-rust/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/Checkout@v5
      