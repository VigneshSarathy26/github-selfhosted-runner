name: Build and push java image

on: 
  push:
    branches:
      - dev
      - test
      - stage
    paths:
      - 'images/runner-java/**'
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/Checkout@v5
      