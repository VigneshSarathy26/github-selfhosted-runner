name: Build and push python image

on: 
  push:
    branches:
      - dev
      - test
      - stage
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/Checkout@v5
      