name: Build job container for Lint Only

on: 
  push:
    branches:
      - dev
      - stage
    paths:
      - "jobcontainers/lint-only/**"
      
jobs:
  build-jc-lint-only:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Login to Container Registry
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build Docker Image 
        uses: docker/build-push-action@v7
        with:
          context: ./jobcontainers/lint-only
          load: true
          push: false
          tags: ${{ vars.REPOSITORY }}:lint-only
      - name: Scanning Docker Image for Vulnerabilities
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ vars.REPOSITORY }}:lint-only
          format: 'table'
          #exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
      - name: Push Image to Artifactory
        run: docker push ${{ vars.REPOSITORY }}:lint-only
