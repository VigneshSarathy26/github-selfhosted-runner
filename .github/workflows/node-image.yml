name: Build and push node image

on: 
  push:
    branches:
      - dev
      - test
      - stage
    paths:
      - 'images/runner-node/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/Checkout@v5
      